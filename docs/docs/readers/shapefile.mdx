---
sidebar_position: 4
sidebar_label: Shapefile
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import CodeFromTest from '@site/src/components/CodeFromTest';
import shapefileExamples from '!!raw-loader!../../tests/python/readers/shapefile_examples.py';
import shapefileScala from '!!raw-loader!../../tests/scala/readers/ShapefileExamples.scala';
import shapefileGbx from '!!raw-loader!../../tests/python/readers/shapefile_gbx_examples.py';

# Shapefile Reader

Both tiers produce the same `(attrs..., geom_0, geom_0_srid, geom_0_srid_proj)` schema — see [Choosing an Execution Tier](../api/execution-tiers#the-one-line-swap).

:::tip Benchmark & tradeoff
The lightweight (`*_gbx`) and heavyweight readers emit the **same schema**, but your
**compute usually decides the tier**: the lightweight tier needs **no JAR or init script**
and is the **only** option on Serverless, standard (shared), and ARM clusters. The
heavyweight tier requires a **classic x86 cluster** (JAR + GDAL init script); where it is
available it uses native GDAL on the JVM and tends to pull ahead on large workloads. See
the [Benchmarking](../api/benchmarking) page for light-vs-heavy timings and methodology.
:::

<Tabs groupId="gbx-tier" queryString="tier">
<TabItem value="lightweight" label="Lightweight · shapefile_gbx">

`shapefile_gbx` is the lightweight Shapefile reader (pyogrio-backed, no JAR). It reads `.shp` and zipped shapefiles and emits the same schema as the heavyweight `shapefile_ogr` reader.

<CodeFromTest code={shapefileGbx} language="python" functionName="READ_SHAPEFILE_GBX"
  source="docs/tests/python/readers/shapefile_gbx_examples.py"
  testFile="docs/tests/python/readers/test_shapefile_gbx_examples.py" />

It is the lightweight counterpart of the heavyweight `shapefile_ogr` reader, supporting Python and SQL bindings (not Scala).

</TabItem>
<TabItem value="heavyweight" label="Heavyweight · shapefile_ogr">

Read ESRI Shapefile format using the `shapefile` reader.

## Format Name

`shapefile_ogr`

## Supported Files

- **`.shp`** - Standard shapefile (requires .shx, .dbf files)
- **`.zip`** - ZIP files containing shapefiles
- Directories with multiple shapefiles

## Basic Usage

### Python

<CodeFromTest
  code={shapefileExamples}
  language="python"
  functionName="READ_SHAPEFILE"
  source="docs/tests/python/readers/shapefile_examples.py"
  testFile="docs/tests/python/readers/test_shapefile_examples.py"
  outputConstant="READ_SHAPEFILE_output"
/>

### Scala

<CodeFromTest
  code={shapefileScala}
  language="scala"
  functionName="READ_SHAPEFILE"
  source="docs/tests/scala/readers/ShapefileExamples.scala"
  testFile="docs/tests/scala/readers/ReadersDocTest.scala"
  outputConstant="READ_SHAPEFILE_output"
/>

### SQL

<CodeFromTest
  code={shapefileExamples}
  language="sql"
  functionName="SQL_SHAPEFILE"
  source="docs/tests/python/readers/shapefile_examples.py"
  testFile="docs/tests/python/readers/test_shapefile_examples.py"
  outputConstant="SQL_SHAPEFILE_output"
/>

## Options

For the full list of options (chunkSize, driverName, layerN, asWKB, etc.), see [OGR reader options](./vector#options).

### Chunk Size

Adjust records per chunk for performance:

<CodeFromTest
  code={shapefileExamples}
  language="python"
  functionName="READ_WITH_OPTIONS"
  source="docs/tests/python/readers/shapefile_examples.py"
  testFile="docs/tests/python/readers/test_shapefile_examples.py"
  outputConstant="READ_WITH_OPTIONS_output"
/>

## Next Steps

- [GeoJSON Reader](./geojson)
- [GeoPackage Reader](./geopackage)
- [FileGDB Reader](./filegdb)
- [API Reference](../api/overview)

</TabItem>
</Tabs>
