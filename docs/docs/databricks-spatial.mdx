---
sidebar_position: 4
title: Databricks Spatial
---

import CodeFromTest from '@site/src/components/CodeFromTest';
import databricksSpatialCode from '!!raw-loader!../tests-dbr/python/databricks_spatial/examples.py';

# Databricks Spatial Types and Functions

Databricks provides **built-in spatial types** and **ST (spatial) and H3 (grid) functions** in Databricks SQL and Databricks Runtime 17.1+. This page gives a concise overview with runnable examples.

GeoBrix is built to complement these native capabilities and bring more data onto them: use GeoBrix readers and writers to move vector/raster data in and out of Databricks `GEOMETRY`/`GEOGRAPHY`, then lean on the built-in ST and H3 functions — alongside GeoBrix RasterX and GridX (e.g. BNG) — for the analysis at scale.

:::info Databricks Runtime required
Examples on this page are validated under `docs/tests-dbr/` and require **Databricks Runtime 17.1+** (or Databricks SQL with ST/H3 support). They will skip when run on open-source Spark.
:::

**References** (cloud-specific Databricks docs):
- **GEOMETRY type** — Euclidean X,Y (and Z); SRID or ANY · [AWS](https://docs.databricks.com/aws/en/sql/language-manual/data-types/geometry-type) | [Azure](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/data-types/geometry-type) | [GCP](https://docs.databricks.com/gcp/en/sql/language-manual/data-types/geometry-type)
- **GEOGRAPHY type** — Lat/lon; SRID 4326 only · [AWS](https://docs.databricks.com/aws/en/sql/language-manual/data-types/geography-type) | [Azure](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/data-types/geography-type) | [GCP](https://docs.databricks.com/gcp/en/sql/language-manual/data-types/geography-type)
- **ST geospatial functions** — Import, export, measurements, predicates, overlay · [AWS](https://docs.databricks.com/aws/en/sql/language-manual/sql-ref-st-geospatial-functions) | [Azure](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/sql-ref-st-geospatial-functions) | [GCP](https://docs.databricks.com/gcp/en/sql/language-manual/sql-ref-st-geospatial-functions)
- **H3 geospatial functions** — Grid indexing with hexagons · [AWS](https://docs.databricks.com/aws/en/sql/language-manual/sql-ref-h3-geospatial-functions) | [Azure](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/sql-ref-h3-geospatial-functions) | [GCP](https://docs.databricks.com/gcp/en/sql/language-manual/sql-ref-h3-geospatial-functions)

---

## GEOMETRY type

**Databricks reference:** [AWS](https://docs.databricks.com/aws/en/sql/language-manual/data-types/geometry-type) | [Azure](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/data-types/geometry-type) | [GCP](https://docs.databricks.com/gcp/en/sql/language-manual/data-types/geometry-type)

Represents a geometric object in a coordinate reference system described by its **SRID**. X,Y (and optionally Z) units depend on the SRID. Use for projected coordinates or when SRID varies.

**Create from WKT, GeoJSON, or WKB:**

<CodeFromTest
  code={databricksSpatialCode}
  language="sql"
  source="docs/tests-dbr/python/databricks_spatial/examples.py"
  testFile="docs/tests-dbr/python/databricks_spatial/test_examples.py"
  functionName="GEOMETRY_CREATE_FROM_WKT"
/>

<CodeFromTest
  code={databricksSpatialCode}
  language="sql"
  source="docs/tests-dbr/python/databricks_spatial/examples.py"
  testFile="docs/tests-dbr/python/databricks_spatial/test_examples.py"
  functionName="GEOMETRY_CREATE_FROM_GEOJSON"
/>

**Export to WKB, GeoJSON, WKT:**

<CodeFromTest
  code={databricksSpatialCode}
  language="sql"
  source="docs/tests-dbr/python/databricks_spatial/examples.py"
  testFile="docs/tests-dbr/python/databricks_spatial/test_examples.py"
  functionName="GEOMETRY_EXPORT"
/>

:::note
About 11000 SRID values are supported.
:::

---

## GEOGRAPHY type

**Databricks reference:** [AWS](https://docs.databricks.com/aws/en/sql/language-manual/data-types/geography-type) | [Azure](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/data-types/geography-type) | [GCP](https://docs.databricks.com/gcp/en/sql/language-manual/data-types/geography-type)

Represents a **geographic** object; coordinates are longitude ([-180, 180]) and latitude ([-90, 90]). Only **SRID 4326** (WGS 84) is allowed.

**Create and export:**

<CodeFromTest
  code={databricksSpatialCode}
  language="sql"
  source="docs/tests-dbr/python/databricks_spatial/examples.py"
  testFile="docs/tests-dbr/python/databricks_spatial/test_examples.py"
  functionName="GEOGRAPHY_CREATE"
/>

<CodeFromTest
  code={databricksSpatialCode}
  language="sql"
  source="docs/tests-dbr/python/databricks_spatial/examples.py"
  testFile="docs/tests-dbr/python/databricks_spatial/test_examples.py"
  functionName="GEOGRAPHY_EXPORT"
/>

---

## ST geospatial functions

**Databricks reference:** [AWS](https://docs.databricks.com/aws/en/sql/language-manual/sql-ref-st-geospatial-functions) | [Azure](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/sql-ref-st-geospatial-functions) | [GCP](https://docs.databricks.com/gcp/en/sql/language-manual/sql-ref-st-geospatial-functions)

Databricks provides **~100 ST spatial SQL functions**. They operate on `GEOMETRY` and/or `GEOGRAPHY`: construct values, export to WKT/GeoJSON/WKB, measure (area, length, distance), access components, test predicates (intersects, contains), overlay (buffer, convex hull, intersection), and transform (affine, reproject by SRID).

**In Python notebooks (Databricks Runtime):**

<CodeFromTest
  code={databricksSpatialCode}
  language="python"
  source="docs/tests-dbr/python/databricks_spatial/examples.py"
  testFile="docs/tests-dbr/python/databricks_spatial/test_examples.py"
  functionName="ST_IMPORT_PYTHON"
/>

**Measurements (area, centroid, distance):**

<CodeFromTest
  code={databricksSpatialCode}
  language="sql"
  source="docs/tests-dbr/python/databricks_spatial/examples.py"
  testFile="docs/tests-dbr/python/databricks_spatial/test_examples.py"
  functionName="ST_MEASUREMENTS"
/>

**Predicates (intersects, contains):**

<CodeFromTest
  code={databricksSpatialCode}
  language="sql"
  source="docs/tests-dbr/python/databricks_spatial/examples.py"
  testFile="docs/tests-dbr/python/databricks_spatial/test_examples.py"
  functionName="ST_PREDICATES"
/>

**Constructors (point, buffer, convex hull):**

<CodeFromTest
  code={databricksSpatialCode}
  language="sql"
  source="docs/tests-dbr/python/databricks_spatial/examples.py"
  testFile="docs/tests-dbr/python/databricks_spatial/test_examples.py"
  functionName="ST_CONSTRUCTORS"
/>

---

## H3 geospatial functions

**Databricks reference:** [AWS](https://docs.databricks.com/aws/en/sql/language-manual/sql-ref-h3-geospatial-functions) | [Azure](https://learn.microsoft.com/en-us/azure/databricks/sql/language-manual/sql-ref-h3-geospatial-functions) | [GCP](https://docs.databricks.com/gcp/en/sql/language-manual/sql-ref-h3-geospatial-functions)

Databricks provides **35+ H3 functions**. H3 is a **global hexagonal grid** with 16 resolutions. Use it to index points and polygons to cell IDs, then aggregate or join on cells (semantic spatial join without geometry predicates).

**In Python notebooks:**

<CodeFromTest
  code={databricksSpatialCode}
  language="python"
  source="docs/tests-dbr/python/databricks_spatial/examples.py"
  testFile="docs/tests-dbr/python/databricks_spatial/test_examples.py"
  functionName="H3_IMPORT_PYTHON"
/>

**Index a point (e.g. NYC; resolution 7 ≈ 5 km²):**

<CodeFromTest
  code={databricksSpatialCode}
  language="sql"
  source="docs/tests-dbr/python/databricks_spatial/examples.py"
  testFile="docs/tests-dbr/python/databricks_spatial/test_examples.py"
  functionName="H3_POINT_INDEX"
/>

**Polyfill (cells contained by a polygon):**

<CodeFromTest
  code={databricksSpatialCode}
  language="sql"
  source="docs/tests-dbr/python/databricks_spatial/examples.py"
  testFile="docs/tests-dbr/python/databricks_spatial/test_examples.py"
  functionName="H3_POLYFILL"
/>

**Neighbors (k-ring, hex ring):**

<CodeFromTest
  code={databricksSpatialCode}
  language="sql"
  source="docs/tests-dbr/python/databricks_spatial/examples.py"
  testFile="docs/tests-dbr/python/databricks_spatial/test_examples.py"
  functionName="H3_NEIGHBORS"
/>

**Cell boundary and center (WKT/GeoJSON):**

<CodeFromTest
  code={databricksSpatialCode}
  language="sql"
  source="docs/tests-dbr/python/databricks_spatial/examples.py"
  testFile="docs/tests-dbr/python/databricks_spatial/test_examples.py"
  functionName="H3_BOUNDARY"
/>

:::note
H3 expressions require Photon-enabled clusters or Databricks SQL (Pro/Serverless). The H3 library is included in Databricks Runtime 11.2+.
:::

---

## Combining with GeoBrix

- **Readers:** Use [GeoBrix readers](/docs/readers/overview) (GDAL, GeoJSON, Shapefile, etc.) to load vector/raster data; then use `st_geomfromwkb(geom_0)` or similar to get Databricks `GEOMETRY`/`GEOGRAPHY` for ST or H3.
- **Writers:** Use [GeoBrix writers](/docs/writers/overview) to export back out — the light `*_gbx` vector writers (GeoJSON, Shapefile, GeoPackage, GeoDatabase) plus raster and PMTiles. To write a Databricks `GEOMETRY`/`GEOGRAPHY` column, coerce it to the writer's WKB + SRID input first with `st_aswkb(geom)` and `st_srid(geom)` (see the [Vector Writer](/docs/writers/vector) schema).
- **GridX:** Discrete global-grid indexing beyond H3 — [GridX functions](/docs/api/gridx-functions) cover **British National Grid** (`bng_polyfill`, `bng_tessellate`, `bng_pointascell`, k-ring/k-loop neighbors and explode variants, cell area/distance, cell set ops with `_agg` aggregates), plus **Quadbin** and **custom user-defined grids** (point→cell, polyfill, tessellate, k-ring, cell geometry/centroid). Use these for BNG or bespoke grids; for a global hex grid use Databricks H3 — and GeoBrix raster→grid aggregation targets both H3 and Quadbin.
- **RasterX:** A full raster toolkit (100+ functions) for the raster workloads Databricks-native spatial doesn't cover — see [Raster Functions](/docs/api/raster-functions): I/O and tiling (GDAL/GeoTIFF, `maketiles`/`retile`/`tooverlappingtiles`, overviews, `resample`/`warp`/`clip`), band math (NDVI, `frombands`, `combine`, `merge`), terrain (`slope`, `hillshade`, `aspect`), focal filters and `viewshed`, and vector↔raster (`rasterize`, `polygonize`, `contour`, DTM-from-geometries, grid-from-points). Aggregate rasters onto grid cells with `rst_h3_rastertogrid*` / `rst_quadbin_rastertogrid*`, then combine with native ST/H3 for zonal stats in SQL.
- **VectorX:** Augments native ST — vector-tile (MVT) encoding (`st_asmvt`, `st_asmvt_pyramid`) for serving web tiles, TIN surface modeling and elevation interpolation (`st_triangulate`, `st_interpolateelevationgeom`), and legacy-geometry migration (`st_legacyaswkb`). Pair with Databricks `GEOMETRY` and the [PMTiles writer](/docs/writers/pmtiles) to publish maps.
