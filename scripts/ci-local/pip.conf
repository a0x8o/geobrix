# pip config baked into the local-act runner image.
# The index-url is injected at image build time from $PIP_INDEX_URL on the host
# (see Dockerfile.gha-runner). Databricks employees should have PIP_INDEX_URL
# exported per go/pypi-registry-access; external contributors get public PyPI.
[global]
timeout = 60
