# pip config baked into the local-act runner image.
# The index-url is injected at image build time from $PIP_INDEX_URL on the
# host (see Dockerfile.gha-runner). Set $PIP_INDEX_URL to point at a private
# mirror if your network requires one; leave it unset to use public PyPI.
[global]
timeout = 60
